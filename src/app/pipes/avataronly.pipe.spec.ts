import { AvataronlyPipe } from './avataronly.pipe';

describe('AvataronlyPipe', () => {
  it('create an instance', () => {
    const pipe = new AvataronlyPipe();
    expect(pipe).toBeTruthy();
  });
});
