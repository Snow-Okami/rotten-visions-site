import { UpdatefilterPipe } from './updatefilter.pipe';

describe('UpdatefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UpdatefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
